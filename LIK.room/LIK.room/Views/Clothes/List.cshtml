<!DOCTYPE html>


        <h1> Католог одягу </h1>
        <h3>@Model.currCategory</h3>
        <div class="row mt-5 mb-2">
    @{
        foreach (var clothing in Model.allClothing)
        {
            <div class="col-lg-4">
            <img class="img-thumbnail" scr="@clothing.Imagine" alt="@clothing.Model"/>
            <h2>Модель: @clothing.Model </h2>
            <p>@clothing.Color</p>
            <p>Ціна: @clothing.Price.ToString(/*"c"*/) грн</p>
            <p><a class="btn btn-warning" href ="#">Детальніше</a></p>
            </div>
        }
    } 
    </div>